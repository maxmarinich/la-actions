{
  "name": "la-actions",
  "version": "1.0.0",
  "scripts": {
    "build:static": "rm -rf static &&cp -r src/ static/ && cp package.json static/"
  },
  "dependencies": {
    "react-alice-carousel": "^2.2.1"
  }
}
